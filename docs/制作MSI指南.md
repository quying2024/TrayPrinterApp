# ?? MSI ??????? x64?

????? Visual Studio Installer Projects?.vdproj?????????????????? MSI ????

?????
1. ??????????x64?
2. ???????? `TrayPrinterApp.Setup` ????
3. ?????? File System ?????????????
4. ???????????
5. ???????? MSI

?????

1. ?????????
- ?????????
  ```powershell
  .\scripts\publish-win-x64.ps1
  ```
- ?????`publish\win-x64`??? `TrayApp.exe` ???????????

2. ??????
- ?????? -> Add -> New Project -> ?? “Setup Project” -> ?? `TrayPrinterApp.Setup`?
- ?? `TargetPlatform` ? `x64`?

3. ?? File System
- ?????? -> View -> File System
- ?? `Application Folder` -> Properties -> DefaultLocation ??? `[ProgramFiles64Folder]\TrayPrinterApp`
- ?? `Application Folder` -> Add -> File -> ?? `publish\win-x64` ??????
- ?? `Application Folder` -> Add -> Folder -> ?? `config`?`docs`?????????
  - `config\appsettings.json`
  - `README.md`, `LICENSE`, `????????.md`, `??????.md`
  - `docs\` ?? md ??

4. ??????
- ? `Application Folder` ??? `TrayApp.exe` -> Create Shortcut
- ???????? `User's Desktop` ? `User's Programs Menu` ?? TrayPrinterApp ????

5. ?? MSI
- ?????? -> Build
- ??? MSI ?? `TrayPrinterApp.Setup\bin\Release`?

???????
- ???????? `scripts\publish-win-x64.ps1` ??????????????
- ?????????????? `TrayApp.csproj` ????????? `Content` ? `CopyToOutputDirectory=PreserveNewest`
- ??? MSI ??????????? SmartScreen ??
